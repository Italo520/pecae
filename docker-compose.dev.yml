version: '3.8'
services:
  api:
    ports:
      - "3333:3000" # Map host 3333 to container 3000 (SERVER_PORT)
  web-frontend:
    ports:
      - "3001:3001" # Map host 3001 to container 3001 (PORT=3001 in Dockerfile)
