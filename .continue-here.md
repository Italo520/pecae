# Handoff de Contexto de Sessão - Pausa de Trabalho (.continue-here.md)

**Data/Hora:** 2026-07-29T16:49:36-03:00  
**Projeto:** PECAÊ (`Italo520/pecae`)  
**Recurso Coolify:** `pecae:main` (UUID `jn38h7vp5ne48tb4668p5mgh`)

---

## 🟢 Status Atual
- **Causa Raiz do Erro 503 / 502 Resolvida**: 
  - O HikariCP/Flyway da API Spring Boot falhava ao tentar obter trava DDL (`pg_advisory_lock`) no PgBouncer do Supabase (porta 6543, pool de transação).
  - Atualizamos `SPRING_DATASOURCE_URL` e `DATABASE_URL` no Coolify para utilizar a porta **5432** (Session Pooler do Supabase).
  - Adicionamos `spring.flyway.postgresql.transactional-lock: false` no `application.yml`.
- **Commit Realizado & Enviado (`push`)**:
  - SHA `4bbacaa`: `fix(flyway): desabilitar transactional-lock no Flyway e ajustar Spring Datasource para Supabase Session Pooler`
- **Deploy em Andamento**:
  - Deploy UUID `ac8he74p0i9q6l75bbpfvqgb` está em andamento no Coolify compilando a versão mais recente (`4bbacaa`).

---

## 🎯 Próximos Passos (Para Retomar a Sessão)
1. Rodar a verificação de status do deploy `ac8he74p0i9q6l75bbpfvqgb` no Coolify:
   - Tool `coolify/list_deployments_by_application` com `uuid: "jn38h7vp5ne48tb4668p5mgh"`.
2. Testar o endpoint REST da API de veículos:
   - `python -c "import urllib.request; print(urllib.request.urlopen('https://api-pecae.italohub.cloud/api/v1/listings?size=20&page=0').read().decode()[:200])"`
3. Confirmar que a listagem de veículos está sendo renderizada no site principal (`https://pecae.italohub.cloud/`).

---

## 📝 Decisões e Conhecimento Relevantes
- Supabase em porta 6543 (Transaction Pooler) bloqueia locks do Flyway e chamadas de `prepareThreshold`.
- Porta 5432 (Session Pooler) suporta todas as conexões JDBC do Spring Boot com segurança.
- O banco Supabase contém **247 veículos cadastrados** prontos para serem servidos pela API.
