# Ponto de Retomada (Handoff)

## Resumo do Contexto Atual
- A Fase 2 (Navegação Deslogada / Guest Access) já está detalhada em `.planning/phases/2-PLAN.md`.
- Acabamos de corrigir o problema das fotos de placeholders sendo exibidas em vez das fotos reais dos veículos no `index.tsx` e `[id].tsx`.
- O limite da requisição na tela inicial foi alterado de 10 para 100 veículos.
- As correções já sofreram commit e push para o repositório (`main`) e estão sendo compiladas pelo Coolify.

## Próximos Passos (Próxima Sessão)
- Iniciar a implementação da Fase 2 com base no `2-PLAN.md`.
- Modificar `useAuthGuard.ts` para capturar a rota de destino (deep linking).
- Atualizar `login.tsx` e `register.tsx` para redirecionarem de volta à origem após login.
- Aplicar bloqueio de UI (Empty State de Acesso Restrito) na tela de Favoritos (`favoritos.tsx`).

## O que rodar ao voltar:
`gsd-resume-work` (ou prosseguir diretamente com a implementação da Fase 2).
