🧪 [testing] Improve MailService test coverage for Resend error handling

🎯 **What:**
Added unit tests to `apps/api/src/mail/mail.service.spec.ts` to cover the `MailService`'s email dispatch logic. Specifically, this fills a testing gap by verifying the behavior of the `try/catch` block inside `sendEmail` when the Resend client encounters an error or throws an exception.

📊 **Coverage:**
- The `try/catch` error re-throwing branch (`if (error instanceof InternalServerErrorException)`) is now explicitly tested.
- Includes tests for Resend API failures causing `InternalServerErrorException`.
- Includes tests for fallback error handling of unexpected exceptions within the `try` block.
- Adds comprehensive coverage for the happy path and debug mode (missing API key).

✨ **Result:**
Significant improvement in unit test coverage for `MailService`, ensuring robust error handling logic during email dispatch is now verified automatically, preventing regressions in future refactors.
