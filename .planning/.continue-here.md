---
context: default
phase: default
task: 3
total_tasks: 4
status: blocked
last_updated: 2026-07-19T23:28:00Z
---

<current_state>
We successfully fixed the Coolify Next.js build failure. The container is now deploying successfully. However, when the user accesses the site, they are getting a "502 Bad Gateway".
</current_state>

<completed_work>

Completed Tasks:
- Task 1: Fix Coolify Build (devDependencies missing) - Done
- Task 2: Configure FQDN in Coolify (pecae.italohub.cloud) - Done
</completed_work>

<remaining_work>

- Task 3: Diagnose and fix 502 Bad Gateway on access - Not started
</remaining_work>

<decisions_made>

- Force `NODE_ENV=development` in Dockerfile during `npm ci` to prevent Coolify from skipping Next.js devDependencies (`@tailwindcss/postcss`, `typescript`).
</decisions_made>

<blockers>
- 502 Bad Gateway: The container starts on port 3000 but the proxy (Traefik) cannot route traffic to it successfully.
</blockers>

## Required Reading (in order)
1. `web-frontend/Dockerfile` — Check the exposed ports and ENV vars.
2. The logs of the `web-frontend` container in Coolify.

## Infrastructure State
- Coolify `web-frontend` app: FQDN is set to `pecae.italohub.cloud`. It's running. `ports_exposes` is 3000.

<context>
The Next.js standalone app is picking up `PORT=3000` (injected by Coolify) and starting successfully, but Traefik might be failing to healthcheck or connect to it for some reason. The user is ending their session now.
</context>

<next_action>
Start with: Investigate Coolify app logs and Traefik proxy logs for the 502 Bad Gateway.
</next_action>
